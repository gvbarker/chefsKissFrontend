// I did this most recent commit just to make sure I could clone the repository easily

// You need to put the following in your console in order to have this website work:

npm install react-router-dom

// You also might have to do this next one if you are having issues, as I had to install the following:
npm install --save react-router react-router-dom

// Need this
node.js installed

// If scripts are not recognized you may need to run this in your terminal
npm install react-scripts --save